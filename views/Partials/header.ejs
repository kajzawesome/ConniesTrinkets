<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title || "Connie’s Trinkets" %></title>
  <link rel="stylesheet" href="/css/style.css">

  <script>
    const loggedIn = <%- JSON.stringify(!!loggedIn) %>;
  </script>

  <script src="/js/script.js" defer></script>
</head>
<body>

<header class="site-header">
  <div class="logo">
    <h2>Connie’s Trinkets</h2>
  </div>

  <nav class="nav-menu">
    <a href="/">Home</a>
    <a href="/market">Items</a>
    <a href="#about">About Her</a>
    <% if (loggedIn) { %>
      <a href="/account">Account</a>
    <% } %>
  </nav>

  <div class="login-button">
    <% if (loggedIn) { %>
      <a href="/logout" class="button small">Logout</a>
    <% } else { %>
      <a href="/login" class="button small">Login</a>
    <% } %>
  </div>
</header>
